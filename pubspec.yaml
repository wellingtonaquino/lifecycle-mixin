name: lifecycle_mixin
description: This package allows you to manage your project using lifecycle through a Mixin.
version: 1.0.0
homepage: https://github.com/wellingtonaquino/lifecycle-mixin

environment:
  sdk: '>=3.0.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  visibility_detector: ^0.4.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1